# This Vite frontend currently has no required environment variables.
#
# If future client-side configuration is needed, add it here with the
# VITE_ prefix and set the matching value in Vercel Project Settings.
#
# Example:
# VITE_PUBLIC_API_URL=https://api.example.com
